//basically extension of main
#include <SFML/Graphics.hpp>

void updateEvents(sf::Event event)//mozda da prosledim i pokazivace na neke stvari da modifikujem???
{
    if(event.type == sf::Event::Closed)
    {
        window.close();
    }

    if(event.type == sf::Event::)
}
